interface:
  display_name: "Writing For Agents"
  short_description: "Make agent-facing documents predictable"
  default_prompt: "Use $writing-for-agents to design or revise this agent-facing document (skill, AGENTS.md, CLAUDE.md, docs)."
policy:
  allow_implicit_invocation: true
