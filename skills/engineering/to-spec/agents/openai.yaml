interface:
  display_name: "To Spec"
  short_description: "Compress a settled thread into one owned spec"
  default_prompt: "Use $to-spec to turn this settled conversation into a destination-first spec with truthful spec-publication state."
policy:
  allow_implicit_invocation: true
