interface:
  display_name: "Prototype"
  short_description: "Run a disposable experiment for one design question"
  default_prompt: "Use $prototype to run a disposable experiment for this one design question."
policy:
  allow_implicit_invocation: true
