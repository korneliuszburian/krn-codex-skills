interface:
  display_name: "Slice Work"
  short_description: "Cut settled work into implementation units"
  default_prompt: "Use $slice-work to turn this settled multi-change spec into vertical slices or explicit migration stages."
policy:
  allow_implicit_invocation: true
