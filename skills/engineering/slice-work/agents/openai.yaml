interface:
  display_name: "Slice Work"
  short_description: "Decompose a settled spec into implement-ready slices"
  default_prompt: "Use $slice-work to turn this settled spec into implementation-ready vertical slices."
policy:
  allow_implicit_invocation: false
