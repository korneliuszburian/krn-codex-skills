interface:
  display_name: "Frontend stage entry"
  short_description: "Enter the frontend stage and know each exit criterion"
  default_prompt: "Use $frontend-stage at the start of a frontend task or when resuming one, to place the work in intake, architecture, build, or verify."
policy:
  allow_implicit_invocation: true
