interface:
  display_name: "Frontend enforcement"
  short_description: "Make CSS conventions binary with lint and CI gates"
  default_prompt: "Use $frontend-enforcement when setting up or running CSS lint, CI quality gates, or a review-rule set."
policy:
  allow_implicit_invocation: true
