# Harness task: slugify

```krn-harness-task
{
  "id": "slugify",
  "prompt": "Make slugify return a lowercase dash-joined slug with no leading or trailing dash.",
  "check": "node check.mjs",
  "workspace": "test/harness/tasks/slugify/workspace"
}
```
