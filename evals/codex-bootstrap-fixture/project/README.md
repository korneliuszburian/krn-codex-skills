# KRN bootstrap fixture

This is a deliberately small target repository used to exercise the installed
KRN CLI through its public `repo inspect` and `repo apply` commands. `LOCAL.md`
is intentionally foreign target-repository prose and must remain untouched.
