{
  "name": "reviewer-quality-subject",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "../../mise-en-palace/node_modules/.bin/tsc -p tsconfig.json",
    "typecheck": "../../mise-en-palace/node_modules/.bin/tsc -p tsconfig.json --noEmit",
    "test": "node dist/test/run-tests.js",
    "verify": "npm run build && npm test"
  }
}
